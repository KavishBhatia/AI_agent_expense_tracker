[project]
name = "expense-tracker-agent"
version = "0.1.0"
description = "An expense tracking agent built with the ADK."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk",
    "pydantic",
    "uvicorn",
    "google-generativeai"
]

[tool.setuptools.packages]
find = {}